<VirtualHost *:80>

    DocumentRoot /var/www/html/public

    Header always set Access-Control-Allow-Origin "https://glpi-vue.vercel.app"
    Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
    Header always set Access-Control-Allow-Headers "Content-Type, Authorization, App-Token, Session-Token, Accept-Language"

    RewriteEngine On

    <Directory /var/www/html/public>
        AllowOverride All
        Require all granted

        # Répondre directement aux preflight OPTIONS, sans passer par PHP
        RewriteCond %{REQUEST_METHOD} OPTIONS
        RewriteRule ^ - [R=200,L]

        # Transmettre Authorization à PHP
        RewriteCond %{HTTP:Authorization} ^(.+)$
        RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

        # Router GLPI (uniquement pour les requêtes non-OPTIONS, donc)
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteRule ^(.*)$ index.php [QSA,L]

    </Directory>

</VirtualHost>